# See https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files for more about ignoring files.

# compiled output
dist
tmp
out-tsc
*.tsbuildinfo
.expo
.expo-shared

# dependencies
node_modules

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# misc
/.sass-cache
/connect.lock
/coverage
/libpeerconnection.log
npm-debug.log
yarn-error.log
testem.log
/typings

# System Files
.DS_Store
Thumbs.db

.cursor/rules/nx-rules.mdc
.github/instructions/nx.instructions.md

# Replit
.cache/
.local/
__pycache__/
*.pyc
# قواعد بيانات SQLite للحالة والسجلات وقت التشغيل — تُنشأ تلقائياً بـ
# CREATE TABLE IF NOT EXISTS عند أول تشغيل، لا تحتوي بيانات مصدر ثابتة
# يحتاجها المشروع، ولا يجوز تتبّعها بـgit (تسبب تضخم تاريخ + تعارضات
# دمج عند كل تشغيل محلي). تحقّقتُ فعلياً من كل واحدة قبل الإضافة هنا.
memory/core_history.db
memory/fable.db
memory/nsm_context.db
memory/chat_history.db
data/mesh.db
data/artifacts.db
.pytest_cache/

# ── أسرار محلية — لا يجوز تتبّعها إطلاقاً ────────────────────────────────
# .env.example فقط هو المقصود بالتتبّع (قالب توثيقي بلا قيم حقيقية).
.env
.env.*
!.env.example
.streamlit/secrets.toml
kaggle.json
**/.kaggle/
*.pem
*.key
id_rsa
id_ed25519
models/transformer_ckg_v3/

# نتائج قياس أداء محلية (تختلف حسب جهاز التشغيل) — السكربت مُتتبَّع، النتيجة لا
benchmark_single_batch_v3_result.json

# مهام Kaggle المحلية المُولَّدة وقت التشغيل
artifacts/model_training/kaggle_jobs/
artifacts/model_training/orchestrator/
artifacts/model_training/architect/
artifacts/model_training/scientist/
artifacts/model_training/meta_ai/
artifacts/model_training/super_ai/
artifacts/model_training/economic/
artifacts/model_training/commercial/
artifacts/model_training/civilization/

# نقاط استئناف التدريب (checkpoints) وسجلات التشغيل والنماذج المُولَّدة محلياً
artifacts/model_training/checkpoints/
artifacts/model_training/training_runs.jsonl
artifacts/model_training/*.pt
artifacts/model_training/*.pkl

.tools/


# جلسات الدفاتر التفاعلية المُولَّدة وقت التشغيل (لا تُرفع — بيانات المستخدم المحلية)
artifacts/model_training/notebooks/
logs/
artifacts/model_training/alerts/
reqtest/

# NSM Runtime Artifacts & Cache
artifacts/cache/
artifacts/agent_loop/audit/
artifacts/learning/distributed_knowledge.json
tests/test_consensus.py
tests/test_reputation.py
tests/test_temporal_recovery.py
artifacts/living_mesh/keys/
artifacts/living_mesh/memory/

# أوزان — لا تُرفع للمستودع (تُحفظ على Kaggle Datasets)
*.pt
*.pth
*.ckpt
*.safetensors
artifacts/hierarchical_moe/*.pt
artifacts/hierarchical_moe/*.npy
ai/models/initial_weights.npy
experiments/**/checkpoints/

# per-node mesh state & keys
artifacts/living_mesh/nodes/
artifacts/living_mesh/network_state.json
node_activity.log
