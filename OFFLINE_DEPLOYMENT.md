# دليل النشر المغلق (بدون إنترنت خارجي)

هذا الدليل موجّه لفريق تقني يشغّل NSM داخل شبكة مغلقة (بدون أي اتصال
بالإنترنت الخارجي)، على بنية تحتية CPU عادي أو GPU صغير. كل ما هنا مبني
على قراءة فعلية للكود — لا افتراضات.

---

## 1. الفكرة العامة

النظام مصمَّم أصلاً ليعمل بسلسلة fallback من عدة مزوّدي LLM سحابيين
(`ai/llm_fallback.py`). في الوضع المغلق، متغيّر بيئة واحد يقطع هذه
السلسلة بالكامل ويحوّل كل الاستدلال إلى نموذج محلي عبر
[Ollama](https://ollama.com):

```
NSM_OFFLINE_MODE=1
```

عند تفعيله:
- **كل** المزوّدين السحابيين (Anthropic, Cloudflare, Gemini, OpenRouter,
  Groq, OpenAI, Together, Hugging Face) يُتجاهَلون بالكامل — حتى لو
  مفاتيح الـAPI الخاصة بهم موجودة في البيئة.
- النموذج المحلي (Ollama) يصبح **المزوّد الوحيد** في السلسلة.
- خمس أدوات أخرى تعتمد على الإنترنت الخارجي (بحث الويب، بحث الصور،
  تحويل نص↔صوت) تتوقف بأدب برسالة عربية موحّدة بدل محاولة اتصال فاشلة
  (`ai/offline_mode.py`).
- إن تعذّر الوصول لخادم Ollama لأي سبب، آخر حلقة في السلسلة تبقى فعّالة
  دائماً بدون أي مفتاح أو اتصال شبكي: **CKG Synthesis** — توليد إجابة
  مباشرة من الرسم المعرفي (Concept Knowledge Graph) نفسه. النظام لا
  "يتعطّل" بالكامل حتى لو Ollama غير متاح مؤقتاً.

---

## 2. التشغيل السريع (موصى به: Docker Compose)

```bash
docker compose up -d
```

هذا الأمر الواحد يشغّل ثلاث خدمات معرّفة في `docker-compose.yml`:

| الخدمة | الدور |
|---|---|
| `ollama` | خادم النموذج المحلي |
| `ollama-pull` | تسحب `qwen2.5:7b-instruct-q4_K_M` تلقائياً عند أول تشغيل فقط (محفوظة في volume دائم، لا تتكرر) |
| `nsm-app` | تطبيق NSM نفسه — لا يبدأ إلا بعد نجاح سحب النموذج |

التطبيق يصبح متاحاً على `http://<عنوان-السيرفر>:8501`.

**متطلبات الجهاز:** Docker + Docker Compose (v2). نموذج 7B مكمَّم
(q4_K_M) يحتاج تقريباً 6–8 GB RAM متاحة أثناء الاستدلال على CPU، ومساحة
قرص ~5 GB لتنزيل النموذج مرة واحدة. الاستدلال على CPU بطيء نسبياً
(ثوانٍ للاستجابة الواحدة) — إن توفّر GPU، Ollama يستخدمه تلقائياً.

**متابعة تقدّم سحب النموذج (أول تشغيل فقط):**
```bash
docker compose logs -f ollama-pull
```

---

## 3. التشغيل بدون Docker (يدوي)

إن لم يكن Docker متاحاً:

```bash
# 1) تثبيت Ollama وسحب النموذج (يحتاج إنترنت في هذه الخطوة فقط،
#    أو نقل ملفات النموذج يدوياً من جهاز متصل)
curl -fsSL https://ollama.com/install.sh | sh
ollama pull qwen2.5:7b-instruct-q4_K_M
ollama serve &

# 2) تثبيت متطلبات بايثون
pip install -r requirements.txt

# 3) تفعيل الوضع المغلق وتشغيل التطبيق
export NSM_OFFLINE_MODE=1
export NSM_LOCAL_LLM_URL=http://localhost:11434   # اختياري، هذا الافتراضي أصلاً
export NSM_LOCAL_MODEL=qwen2.5:7b-instruct-q4_K_M  # اختياري، هذا الافتراضي أصلاً
streamlit run streamlit_app.py --server.port 8501
```

---

## 4. متغيرات البيئة الكاملة (وضع مغلق)

مصدر كل متغيّر أدناه هو استدعاء `os.getenv()` فعلي في الكود — مو تخمين.

| المتغيّر | الغرض | افتراضي | إجباري؟ |
|---|---|---|---|
| `NSM_OFFLINE_MODE` | تفعيل الوضع المغلق بالكامل | (فارغ = معطّل) | **نعم** للوضع المغلق |
| `NSM_LOCAL_LLM_URL` | عنوان خادم Ollama | `http://localhost:11434` | لا |
| `NSM_LOCAL_MODEL` | اسم النموذج المحلي | `qwen2.5:7b-instruct-q4_K_M` | لا |

مصدر: `ai/llm_fallback.py` (تعليق رأس الملف + الأسطر 468–532)،
`ai/offline_mode.py`.

---

## 5. ما يعمل تلقائياً وما يتوقف

### يعمل بدون إنترنت (بعد تفعيل `NSM_OFFLINE_MODE=1`)
- الدردشة الأساسية (سلسلة: Ollama المحلي → CKG Synthesis كحل أخير مضمون)
- تصفّح الرسم المعرفي (CKG) وكل بيانات القرآن المحلية
- سجل المحادثات (SQLite محلي بالكامل، `memory/nsm_context.db`)
- OCR (`pytesseract` + `tesseract-ocr-ara`، محلي بالكامل)

### يتوقف بأدب برسالة عربية واضحة (لا محاولة اتصال فاشلة)
- `ai/web_search_tool.py` — بحث الويب
- `ai/image_sources.py`, `ai/image_search_tool.py` — بحث الصور التوضيحية
- `ai/stt_engine.py`, `ai/tts_engine.py` — تحويل نص↔صوت

### ⚠️ فجوة معروفة لم تُحَل بعد: الصوت (TTS/STT)
حتى المحركات المذكورة في الكود كـ"بدون مفتاح" (`edge-tts`, `gTTS`)
**تتصل فعلياً بخوادم مايكروسوفت/جوجل** — ليست محلية حقيقةً. بالوضع
المغلق، ميزة الصوت **معطّلة بالكامل حالياً**، وليست مجرد "معطّلة بأدب
مؤقتاً". لتفعيلها فعلياً داخل شبكة مغلقة يلزم تكامل محرك محلي حقيقي —
مثل [Piper TTS](https://github.com/rhasspy/piper) أو
[whisper.cpp](https://github.com/ggml-org/whisper.cpp) — وهذه مهمة
تطوير منفصلة، غير مُنفَّذة في هذا الدليل.

### ملاحظة: Qdrant غير مؤثِّر أصلاً
`ai/vector_backend.py` (قاعدة متجهات Qdrant Cloud) موجود ومستورَد جزئياً
عبر `nsm_memory.py`، لكن بالتتبّع الفعلي لكل نقاط الاستدعاء في المسار
الحي (`streamlit_app.py`) — لا يوجد أي استدعاء فعلي للدوال التي تلامس
Qdrant (`save`, `semantic_search`, `upsert_fact`, `search_facts`).
عملياً: لا حاجة لضبط `QDRANT_URL`/`QDRANT_API_KEY` في الوضع المغلق —
هذا الجزء من الكود ميت بالممارسة بغض النظر عن وجود إنترنت من عدمه.

---

## 6. التحقق من التشغيل الصحيح

```bash
# فحص صحة التطبيق (نفس ما يستخدمه HEALTHCHECK في Dockerfile)
curl -f http://localhost:8501/_stcore/health

# فحص أن Ollama يرى النموذج المسحوب
curl http://localhost:11434/api/tags
```

داخل التطبيق: افتح تبويب الدردشة واسأل سؤالاً بسيطاً. إن كانت الإجابة
تصل خلال ثوانٍ عبر النموذج المحلي، الإعداد سليم. إن كانت الإجابات تأتي
فقط من CKG Synthesis (نمط جمل أكثر جفافاً/قالبية)، راجع الجدول أدناه.

---

## 7. استكشاف الأخطاء الشائعة

| العرض | السبب المحتمل | الحل |
|---|---|---|
| الإجابات قوالبية جداً (CKG Synthesis فقط) | Ollama غير متاح أو النموذج لم يُسحب بعد | `docker compose logs ollama-pull` |
| بطء شديد في الاستجابة | استدلال 7B على CPU بدون GPU | طبيعي جزئياً؛ فكّر بنموذج أصغر أو GPU |
| رسالة "تعذّر تحميل سجل المحادثات" | صلاحيات الكتابة على مجلد `memory/` | تأكد أن الـvolume `nsm_memory` قابل للكتابة |
| ميزة الصوت لا تعمل نهائياً | متوقّعة حالياً — انظر القسم 5 | يحتاج تطوير محرك محلي (خارج نطاق هذا الدليل) |

---

## 8. ما هو خارج نطاق هذا الدليل

- تكامل TTS/STT محلي حقيقي (Piper/whisper.cpp) — مهمة تطوير منفصلة.
- تنظيف حجم المستودع (~2.1GB بسبب تاريخ Git القديم) — لا يؤثر على
  التشغيل، فقط على سرعة أي `git clone` مستقبلي.
- الجوانب التعاقدية/القانونية للتسليم للجهة المشترية.
