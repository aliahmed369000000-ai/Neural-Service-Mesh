نفّذ الإصلاحات التالية بالترتيب، كل واحدة على حدة، اختبرها فعليًا قبل
الانتقال للتالية، واعمل commit + push بعد كل إصلاح ناجح (لا تجمّعها
بـcommit واحد ضخم في النهاية).

⚠️ هذه إصلاحات محددة ومؤكدة مسبقًا — لا تضف أي "تحسينات" إضافية غير
مذكورة هنا، ولا تعيد كتابة ملفات كاملة إذا يكفي تعديل مستهدف.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1) إصلاح ثغرة حقيقية بـ ai/social_platforms/youtube_adapter.py
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
الدالة _latest_video_id() تصل مباشرة لـ:
  os.environ["YOUTUBE_API_KEY"]
  os.environ["YOUTUBE_CHANNEL_ID"]
بدون أي فحص is_configured()/required_env أولاً — إذا كانت غير موجودة
سترفع KeyError خام بدل رسالة واضحة. أضف فحصًا في بداية الدالة يرفع
NotConfiguredError (نفس النمط المستخدم بباقي الدالة بهذا الملف وبباقي
المحولات بنفس المجلد) إذا كانت YOUTUBE_API_KEY أو YOUTUBE_CHANNEL_ID
غير موجودتين، قبل أي وصول لـ os.environ[].

اختبر: استدعِ الدالة بدون هذين المتغيرين وتأكد أنها ترفع NotConfiguredError
مو KeyError.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2) فصل أوزان ai/neural_weights.py عن سلسلة الاستيراد الإجباري
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ai/__init__.py يستورد ai.neural_weights (~5MB كـPython literals) بشكل
إجباري ضمن ~15 استيراد أخرى، مما يُبطئ أي "import ai" في المشروع.

المطلوب: حوّل الأوزان المضمّنة داخل neural_weights.py من Python
literals إلى ملف بيانات خارجي (.npy أو .json) يُحمَّل عند الحاجة
الفعلية فقط (lazy loading) داخل الدالة/الكلاس التي تستخدمه، بدل ما
يُحمَّل تلقائيًا عند كل "import ai". لا تغيّر منطق الحساب نفسه، فقط
طريقة تحميل البيانات.

اختبر: قِس وقت "import ai" قبل وبعد التعديل وتأكد من تحسّن ملموس،
وتأكد أن أي كود يستخدم NeuralWeightLayer/get_default_layer ما زال
يعمل بنفس النتائج.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3) حذف الملف المكرر ai/governance_p7.py
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ai/governance.py و ai/governance_p7.py متطابقان بايت لبايت (تأكّدت
بـdiff مسبقًا). قبل الحذف: ابحث في كل المشروع (grep) عن أي استيراد
لـ governance_p7 تحديدًا (from ai.governance_p7 import ...) وحوّله
ليستورد من ai.governance بدلاً منه، ثم احذف governance_p7.py.

اختبر: تأكد أن كل الاستيرادات تعمل بعد الحذف (py_compile لكل الملفات
المتأثرة على الأقل).

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
4) توضيح أن ai/phase6_validator.py بيانات وهمية
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
الملف بأكمله (24 سطر) يُرجع دائمًا score=85.0 و"Phase 7 ready" ثابتة،
بدون أي فحص حقيقي. لا تحاول "تصليحه" ببناء فاليديتور حقيقي (خارج
النطاق الآن) — فقط:
  أ) أضف تعليق تحذيري واضح بأعلى الملف: "⚠️ STUB — نتائج ثابتة وهمية،
     ليست فحصًا حقيقيًا"
  ب) إذا كانت نتيجته تظهر بأي مكان بواجهة streamlit_app.py للمستخدم،
     أضف بجانبها تنبيهًا مرئيًا (مثل badge "تجريبي/وهمي") بدل عرضها
     كأنها نتيجة حقيقية.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
بعد كل إصلاح: git add + commit برسالة عربية واضحة + push مباشرة،
وأخبرني في كل مرة هل نجح الرفع أم لا.