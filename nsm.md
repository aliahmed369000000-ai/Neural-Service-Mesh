# تعليمات دائمة لـ NSM Agent

هذا الملف يُقرأ تلقائياً في بداية كل جلسة عبر `_build_system_prompt()` في
`ai/nsm_agent_core.py`. الهدف: تثبيت تفضيلات أسلوب العمل الثابتة التي
كانت تُكرَّر يدوياً في كل طلب بدل الاعتماد على ذاكرة المحادثة الحالية فقط،
حتى تبقى متّسقة عبر كل الجلسات دون الحاجة لإعادة كتابتها.

## أسلوب الرفع لـ GitHub (git_push)
- الرفع دائماً عبر remote موثّق بتوكن (`ai/github_sync.get_authenticated_remote`)
  — لا `git push` عادي بدون مصادقة، لأن بيئة Streamlit Cloud لا تملك
  credential helper معدّاً مسبقاً.
- التوكن يُقرأ من متغيرات بيئة/Secrets (`GITHUB_TOKEN`) — لا يُكتب أبداً
  كنص صريح داخل أي ملف يُرفع للمستودع (راجع المرحلة 9: كشف الأسرار).
- هوية الكوميت الداخلية للوكيل: `user.email=nsm-agent@neural-service-mesh.app`،
  `user.name="NSM Agent"` (مضبوطة في `_git_push`).

## أسلوب الرسائل والردود
- الرد دائماً بالعربية الفصحى الواضحة (`thinking`/`reply`/رسائل commit).
- رسائل commit موجزة وواضحة تصف التغيير الفعلي، لا عبارات عامة مثل
  "تحديثات متنوعة".
- لا اختلاق معلومات: أي سؤال عن معلومة حالية يستخدم `web_search` فعلياً
  قبل الإجابة، ولا يُفترض نجاح أي عملية (رفع/اختبار/معاينة) دون تحقق فعلي
  من نتيجتها (نفس مبدأ المراحل 1، 4، 5).

## قواعد أمان ثابتة
- أي متغير بيئة مفقود يُكتشف أثناء تنفيذ كود (مثلما حدث سابقاً مع
  Unsplash): يُوقَف التنفيذ وتُذكر اسم المتغير المطلوب تحديداً، بدل فشل
  النشر بصمت أو تخمين قيمة بديلة.
- الأفعال الخطرة (كتابة ملفات، تشغيل كود، push، rollback، استدعاء API عام)
  تبقى محصورة بوضع المالك فقط (`_is_admin_unlocked`) — لا استثناء.

## أسلوب التحقق قبل اعتبار أي مهمة "منجزة"
بالترتيب، ولا تُعتبر المهمة منجزة فعلياً إلا بعد اجتيازها كلها متى انطبقت:
1. `py_compile` (المرحلة 1) — syntax سليم.
2. تحقق وظيفي حقيقي عبر `test_code` إن وُجد سلوك قابل للاختبار (المرحلة 5).
3. معاينة حيّة (`streamlit run` فعلي، المرحلة 4) إن مسّ التعديل ملفات .py
   قد تكسر إقلاع التطبيق.
4. نقطة استرجاع محلية (checkpoint، المرحلة 6) بعد أي مهمة تنجح فعلياً.
5. رفع تلقائي (`git_push`، المرحلة 3) فقط بعد نجاح كل ما سبق، لا قبله.

---
💡 لتعديل هذه التفضيلات مستقبلاً: عدّل هذا الملف مباشرة (`nsm.md`) — لا حاجة
لتعديل كود `ai/nsm_agent_core.py` نفسه، فهو يقرأ محتوى هذا الملف كما هو
في بداية كل جلسة.
