# requirements-verifier.txt
# =========================
# اعتماديات اختيارية لوحدة ai/nsm_answer_verifier.py (فحص تأسيس إجابات
# knowledge/qa_engine.py على المصدر عبر DeepEval FaithfulnessMetric).
#
# لماذا ملف منفصل عن requirements.txt الأساسي:
#   - غير مطلوب لتشغيل التطبيق نفسه على Streamlit Community Cloud —
#     محدود الذاكرة أصلاً (انظر ملاحظات المشروع)، وDeepEval يجلب
#     اعتماديات إضافية غير لازمة للمنتج في وقت التشغيل الفعلي.
#   - يُستخدم فقط اختيارياً (include_faithfulness_check=True) أو مباشرة
#     من ai/nsm_answer_verifier.py، عادة في اختبارات محلية/CI أو تدقيق
#     يدوي لجودة الإجابات — وليس في مسار كل طلب مستخدم.
#
# التثبيت المحلي:
#   pip install -r requirements-verifier.txt
#   # الحَكَم (judge LLM) مجاني بالكامل عبر ai/free_router.py — يكفي
#   # مفتاح واحد فقط من التالية (بلا أي اشتراك مدفوع):
#   export GROQ_API_KEY=...        # أو
#   export GOOGLE_API_KEY=...      # أو
#   export CF_API_TOKEN=... ; export CF_ACCOUNT_ID=...
#
# بدون هذا الملف مثبَّتاً، أو بدون أي مفتاح مما سبق: ai/nsm_answer_verifier.py
# يتدهور بأمان كامل (verify_answer_faithfulness يُرجع
# {"available": False, ...} بلا أي استثناء أو كسر لبقية النظام).
deepeval>=3.0.0
